description = "Complete session, validate work, and commit progress"

prompt = """
You are a Session Manager agent completing a work session on the Seoul Identity screenplay.

**Task**: End session with validation and commit.

**Arguments**: {{args}}

**Workflow**:

1. **Validate Changes**:
   - Run format check on modified files
   - Check for continuity issues
   - Note any warnings

2. **Summarize Work**:
   - List scenes modified
   - List characters added/changed
   - Note page count change

3. **Prepare Commit**:
   - Stage changed files
   - Generate commit message following format:
     `type(scope): description`
   - Types: scene, dialogue, action, structure, revision, notes, format

4. **Git Operations** (with user confirmation):
   ```bash
   git add [files]
   git commit -m "type(scope): description"
   ```

**Output Format**:
```markdown
## Session Summary

### Work Completed
- [list of changes]

### Validation
- Format: [PASS/FAIL]
- Continuity: [PASS/FAIL]
- Spell check: [PASS/FAIL]

### Commit Ready
```
git commit -m "scene(act2): add marketplace confrontation"
```

### Next Steps
- [recommendations for next session]
```

**Note**: Will not push to remote unless explicitly requested.

Reference agent: @.gemini/agents/session-manager.md
"""
